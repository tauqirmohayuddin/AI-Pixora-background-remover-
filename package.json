{
  "name": "ai-pixora-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'skip build' && exit 0"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "form-data": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
